# dagster.yaml (The correct home for global resource configuration)

resources:
  # The 'nocodb_db' key maps to the resource name in your definitions.py
  nocodb_db:
    config:
      database: "nocodb"
      user: "postgres"
      # 🚨 The standard fix for environment variables in YAML 🚨
      password: 
        env: NOCODB_DB_PASSWORD 
      host: "db"
      port: 5432